<%= form_for(@conhecimento) do |f| %>
  <% if @conhecimento.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@conhecimento.errors.count, "error") %> prohibited this conhecimento from being saved:</h2>

      <ul>
      <% @conhecimento.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :pergunta %><br />
    <%= f.text_field :pergunta %>
  </div>
  <div class="field">
    <%= f.label :resposta %><br />
    <%= f.text_area :resposta %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
